# JitPack Configuration for AndroidTestEngine
jdk:
  - openjdk11

before_install:
  - sdkmanager "platforms;android-34" "build-tools;34.0.0" "sources;android-34"

install:
  - ./gradlew :library:publishToMavenLocal
